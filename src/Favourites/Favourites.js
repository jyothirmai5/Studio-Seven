import React, { useEffect, useState } from 'react';
import Layout from '../Layout/Layout';

function Favourites() {
    return (
        <Layout>
            <h1>Favourites</h1>
        </Layout>);
}

export default Favourites;